name: offline_sync_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  # Local Database
  hive: ^2.2.0
  hive_flutter: ^1.1.0
  # Firebase
  firebase_core: ^2.24.0
  cloud_firestore: ^4.14.0
  # Connectivity
  connectivity_plus: ^5.0.0
  # State Management
  provider: ^6.1.0
  # Utilities
  uuid: ^4.0.0
  path_provider: ^2.1.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.0
  build_runner: ^2.4.0
  mockito: ^5.4.4

flutter:
  uses-material-design: true
